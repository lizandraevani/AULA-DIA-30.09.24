// sort(): Ordena os elementos de um array. Por padrão, o sort() ordena os elementos como strings, o que pode causar uma ordenação numérica incorreta.

let numbers = [40, 100, 1, 5, 25, 10];
numbers.sort();
console.log(numbers); // saida: [1, 10, 100, 25, 40, 5]